# https://www.hackerrank.com/challenges/30-operators/problem

meal = float(input())
tip = float(input())
tax = float(input())

total = round((meal * (tip/100)) + (meal * (tax/100)) + meal)
print(total)
